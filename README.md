# SQL-Query-Generator
This SQL Query Generator project was related to natural language processing in this particular project was actually contain to .pynb language.for the most sql project related to this.in the interactive project i was developed to implement with Google colab and create a new notebook that is displays in the screen.and install the Pip install -U transformers in the particular content page then i runn in to the play button so it has implemented in some way to give the output like sql generator object functions.

In the project has full declared .pynb code for python development the code was running to develop the sql generator project.
The project is organized into logical "cells" to ensure a reproducible and modular workflow. This allows for easy debugging and iterative testing of different natural language prompts.

This the project translate natural language to  Sql Language for machine that has known the particular language to implement for user they want the code of project like sql database methods projects.

click the RunAll the project and it is shown the link of URL like Gradio.live page.
Connect to touch the link and displays the project solution.then i ask some natural  language to the next  to generate sql object box it  was give me one some sql language for database creation.
